<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wool.board">
	<select id="boardSelectAll" resultType="BoardDTO">
	select bno, title, contents, writingTime, memberId, hit, recommend from board
	ORDER BY bno DESC
	</select>
	
	<select id="boardSelect" resultType="BoardDTO" parameterType="BoardDTO">
	select bno, title, contents, writingTime , memberId , hit , recommend from board
	where bno = #{bno}
	</select>
	
	<insert id="boardInsert" parameterType="BoardDTO">
	insert into board
	(bno, title, contents, writingTime , memberId, hit , recommend)
	values
	(Board_SEQ.NEXTVAL , #{title}, #{contents}, TO_CHAR(SYSDATE, 'YYYY.MM.DD HH24:MI:SS') , #{memberId} , 0 , 0)
	</insert>
	
	<update id="boardUpdate" parameterType="BoardDTO">
	update board set
 	title = #{title}, contents = #{contents} , MODIFIEDTIME = TO_CHAR(SYSDATE, 'YYYY.MM.DD HH24:MI:SS')
 	where bno = #{bno}
	</update>
	<update id="boardHit">
	update board set
	hit = hit+1
	where bno = #{bno}
	</update>
	
	<delete id="boardDelete" parameterType="int">
	delete from board where bno = #{bno}
	</delete>
	
	
</mapper>